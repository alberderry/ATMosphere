import React from 'react';
import { Box } from '@chakra-ui/react';
import Submission from '../components/submission/Submission.jsx'; // Corrected import to Submission component

const SubmissionPage = () => {
    return (
        <Box>
            <Submission />
        </Box>
    );
};

export default SubmissionPage;
